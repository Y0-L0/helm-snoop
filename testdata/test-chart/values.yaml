fullnameOverride: nil
nameOverride: nil

config:
  message: "hello"
  enabled: true
  disabled: null

integerKey:
  1234: "boom"

getPipe:
  someKey: someValue

indexPipe:
  someKey: someValue
  nestedKey:
    someOtherKey: nestedPipedValue

extra:
  fromDefine: fromValues

image:
  registry: docker.io
  repository: myapp
  tag: latest

# PostgreSQL configuration with existingSecret pattern
postgresql:
  auth:
    password: mypassword
    existingSecret:
      name: my-custom-postgres-secret
      keyMapping:
        password: pg_password
